# forgetted

> Selective memory governance for AI agents. Branch the conversation timeline — keep context, block persistence.

forgetted gives AI agents an "incognito mode." During a forgetted window, the agent has full context but writes nothing to memory files, session logs, vector DBs, or deliverables. When the window closes, everything is cleaned up as if it never happened.

Key properties:
- builtins.open monkey-patch catches writes from any Python layer
- Adapter pattern for extensibility (file writes, mem0, Hindsight, CrewAI, custom)
- No-op returns instead of raising — agent code doesn't crash
- Idempotent: double-start, stop-before-start, double-stop are safe

Stack: Python 3.9+, zero required dependencies. Optional mem0, Hindsight and CrewAI adapters (duck-typed; no framework import).

## Docs
- [README](README.md): Architecture, threat model, quick start, adapter guide
- [CHANGELOG](CHANGELOG.md): Version history

## Code
- [Session](forgetted/session.py): ForgetSession orchestrator (main entry point)
- [Guard](forgetted/guard.py): ForgetGuard — builtins.open monkey-patch
- [Trigger](forgetted/trigger.py): Detect forgetted activation in user messages
- [Checkpoint](forgetted/checkpoint.py): Save/load resumption files
- [Cleaner](forgetted/cleaner.py): Find and delete session logs
- [Adapters](forgetted/adapters/): PersistenceAdapter ABC + file_write, mem0, and native (Hindsight/CrewAI) adapters

## Testing
- 116 tests (113 pass, 3 xfail) covering triggers, guard, checkpoint, cleaner, session, adapters, and adversarial vectors. The xfail cases document known bypass paths (Path.write_text / Path.write_bytes do not go through builtins.open; overlapping sessions stopped out of order break the guard chain).

## Optional
- [License](LICENSE): Apache 2.0
- [Agent Instructions](AGENTS.md): Instructions for AI coding agents working on this repo


## About Hermes Labs

Hermes Labs studies failure modes in agent and LLM systems, develops open-source tools that treat language as part of the runtime, and works with teams to remediate reliability failures in production. More at [hermes-labs.ai](https://hermes-labs.ai).
